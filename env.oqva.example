# env.oqva.example — EXTRA variables for the OQVA fork
#
# Merge into .env as needed.
# Base .env comes from deployments/cli/community/variables.env (or .env.example).
# Here only what OQVA adds or overrides.

# Cloudflare Tunnel: connector token. If set, run-oqva.sh enables the "tunnel" profile.
# TUNNEL_TOKEN=

# Public app URL (e.g. https://plane.example.com). Used as base for VITE_* in the build.
# Required when behind a tunnel or custom domain.
# WEB_URL=

# API: default project member — UUID of user to add to every new project.
# If not in workspace, user is added to workspace first (role Member), then to project.
# Leave empty to disable.
# DEFAULT_PROJECT_MEMBER_ID=c40745b2-cbfc-43c3-915f-d1513f8e2b5d

# Task Breakdown (AI breakdown service)
# Public URL for breakdown API (e.g. https://plane.example.com/breakdown/api). Used by web build.
# TASK_BREAKDOWN_API_URL=
# API key for breakdown (used by frontend and breakdown service). Same value for both.
# TASK_BREAKDOWN_API_KEY=
# Plane API URL and key (breakdown service calls Plane API).
# - Docker: use http://api:8000 (internal hostname). localhost does NOT work from inside container.
# - Local dev (breakdown on host): use http://localhost:8000
# PLANE_API_URL=https://api.plane.so
# PLANE_API_KEY=  # Required. Create in Plane: Settings → API Tokens
# AI provider — at least one required (Anthropic, OpenAI, or Google for Gemini)
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# GOOGLE_API_KEY=
