# Plane API (required for breakdown to fetch work items and create tasks)
# Docker: use http://api:8000. Local dev: use http://localhost:8000
PLANE_API_URL=https://api.plane.so
# Required. Create in Plane: Settings → API Tokens
PLANE_API_KEY=

# Breakdown API key (required - used by frontend to authenticate)
BREAKDOWN_API_KEY=

# AI provider - at least one required (Task Master uses for breakdown/expand)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# GOOGLE_API_KEY=  # Optional, for Gemini

# Optional
MAX_TASKS_DEFAULT=10
TASK_MASTER_TIMEOUT_MS=120000
TASK_MASTER_STORAGE_PATH=
